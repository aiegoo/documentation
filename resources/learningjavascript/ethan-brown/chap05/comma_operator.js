let x = 0, y = 10, z;
z = (x++, y++);

console.log(z)

/* The comma operator provides a simple way to combine expressions: it simply evalu‐
ates two expressions and returns the result of the second one.  */